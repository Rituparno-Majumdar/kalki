"""
Kalki data connectors package.

Each connector fetches, validates, and normalises data from a single
Indian government data source into the standard Kalki schema.

See base_connector.py for the interface every connector must implement.
See example_connector.py for a fully-annotated reference implementation.
"""
